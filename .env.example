# Safe to expose in the browser. Copy to .env.local for local development.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me

# Never prefix CLERK_SECRET_KEY with VITE_ and never commit it.
# Server-side Clerk work must store it as an encrypted Cloudflare secret.
